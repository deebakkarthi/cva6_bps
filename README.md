# cva6_bps

## Steps to run

1. Use `git clone https://github.com/deebakkarthi/cva6` to obtain the specific version used in this evaluation
2. Set the environment variable `$CVA6_REPO_DIR` to point to where you downloaded the `cva6` repo
3. Run `jg -tcl cva6.tcl` to run Jasper Gold
